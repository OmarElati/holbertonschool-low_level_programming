#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

unsigned int binary_to_uint(const char *b);
unsigned int _pow(unsigned int x, unsigned int y);

#endif /* MAIN_H */
