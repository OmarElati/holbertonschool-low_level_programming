#include <stdio.h>
 /**
  * main - Print name followed by new line
  * @argc: Number command line arg
  * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
    printf("%s\n", argv[0]);
    return (0);
}
